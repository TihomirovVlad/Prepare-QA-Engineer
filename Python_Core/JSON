#JSON = dict/list
#Сервер присылает json -> я паршу через response.json() -> получаю dict
#Обязательно проверять все поля, типы, значения
#amount должен быть int, а не float(чтобы избежать ошибок с копейками: числа после ",")